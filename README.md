# laravel-safe-migrations

Safer database migrations for Laravel: rollback guard, schema snapshots, and an expand/contract workflow.

## Install

```bash
composer require octowire/laravel-safe-migrations
